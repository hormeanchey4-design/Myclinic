# Copy this file to .env (or set these values in your hosting control panel).
# This project reads values using getenv(), so environment variables are preferred in production.

DB_HOST=localhost
DB_USER=clinic_user
DB_PASS=change_this_password
DB_NAME=clinic_system

# Example:
# APP_BASE_URL=https://your-domain.com/clinic_system/
APP_BASE_URL=https://your-domain.com/clinic_system/
